<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.truynkhngci">

    <application
        android:allowBackup="true"
        android:name=".View.App"
        android:icon="@drawable/icon_avt"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TruyệnKhôngCười">
        <activity
            android:name=".View.m000_flash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.m001_main_act"/>
        <activity android:name=".View.m002_viewPager" />
    </application>

</manifest>